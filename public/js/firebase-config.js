// Deprecated: Firebase is now initialized via Firebase Hosting auto-config
// in `public/index.html` using `/__/firebase/init.js`.
//
// This file is intentionally kept as a no-op so older cached pages that still
// request it won't break, but it no longer contains any config values.
window.firebaseApp = window.firebaseApp || firebase;
