{
  "name": "shoso-photobook",
  "version": "1.0.0",
  "description": "Photo Book Creator - Create beautiful photo books from Google Photos",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "firebase emulators:start",
    "deploy": "npm run build && firebase deploy",
    "deploy:hosting": "npm run build && firebase deploy --only hosting",
    "deploy:functions": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "@google-cloud/vision": "^5.3.4",
    "dotenv": "^17.2.4",
    "firebase": "^12.6.0",
    "gsap": "^3.14.2",
    "node-fetch": "^3.3.2",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "@daybrush/utils": "^1.13.0",
    "@scena/matrix": "^1.1.1",
    "firebase-tools": "^13.0.0",
    "moveable": "^0.53.0",
    "vite": "^7.3.1"
  }
}
